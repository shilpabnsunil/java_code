package programs;

public class ObjectClass extends Object{
	
	public static void main(String[] args) {
		
	    //created object
		//we can create relation with object with 2 ways 
		//implicitly(itself) and explicitly(forcefully)
	ObjectClass obj=new ObjectClass();

	}
}
